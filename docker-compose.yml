services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4000
    ports:
      - "127.0.0.1:4000:4000"
    mem_limit: 384m
    cpus: "0.75"
